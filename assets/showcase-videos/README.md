# Showcase Videos

Qualitative rollout clips for the website. `manifest.json` indexes them and `showcase.js` plays them.

Families, in display order: VideoPlaceOrder, VideoRepick, PatternLock.

63 clips: 3 π₀.₅ no-history clips plus 60 memory-system condition clips. Every clip matches the success/fail label of its canonical rollout row.

RouteStick is excluded from the showcase: its recurrent-TTT k0 clip did not reliably reproduce the saved canonical outcome under the video runtime, so it was dropped rather than shown with a mismatched label.
